# PassWord_Generator
Bash script (CLI) to generate secure and random passwords. Allows customizable length (8-32) and character sets (uppercase, numbers, symbols). Automatically saves the result to a local text file.
